{
  "name": "attraktor-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:app": "cd app && npm run dev",
    "dev:strapi": "cd strapi && npm run develop",
    "dev": "concurrently \"npm:dev:*\"",
    "postinstall": "(cd app && npm install) && (cd strapi && npm install)"
  },
  "author": "Attraktor e.V.",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^8.2.2",
    "qs": "^6.12.3"
  }
}
